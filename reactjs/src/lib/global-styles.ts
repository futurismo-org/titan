import { createGlobalStyle } from 'styled-components';

const GlobalStyle = createGlobalStyle`@import url('https://fonts.googleapis.com/css?family=M+PLUS+1p');
body{
  font-family: 'M PLUS 1p' !important;
}`;

export default GlobalStyle;
