import React from 'react';

const Review = (props: any) => {
  const { userReview, loading } = props;
  return (
    <React.Fragment>
      <p>test</p>
    </React.Fragment>
  );
};

export default Review;
